1.变量：就是内存中的存储空间，空间中存储着经常发生改变的数据。
  数据类型 变量名 = 数据值
  int salary = 15000;

  注意事项：
    （1）变量名不允许重复定义
    （2）一条语句，可以定义多个变量，中间用逗号分隔
      int a=1,b=2,c=3;
    (3)变量在使用之前，必须完成赋值。
2.键盘输入
        Scanner sc= new Scanner(System.in);
        int age=sc.nextInt();
        System.out.println(age);
